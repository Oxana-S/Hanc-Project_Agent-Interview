# 🐍 Установка с Python 3.14

У вас установлен **Python 3.14**, который очень новый. Некоторые пакеты могут не поддерживать его.

## Вариант 1: Используйте минимальные зависимости (РЕКОМЕНДУЕТСЯ)

```bash
# Удалите старое окружение если было
rm -rf venv

# Создайте новое
python3 -m venv venv
source venv/bin/activate

# Установите минимальный набор
pip install --upgrade pip
pip install -r requirements-minimal.txt
```

Затем запустите:
```bash
python main.py
```

## Вариант 2: Установите Python 3.11-3.13 через pyenv

```bash
# Установите pyenv (если ещё не установлен)
curl https://pyenv.run | bash

# Установите Python 3.12
pyenv install 3.12.0

# Используйте для проекта
pyenv local 3.12.0

# Создайте окружение
python -m venv venv
source venv/bin/activate

# Установите все зависимости
pip install -r requirements.txt
```

## Вариант 3: Используйте Docker (без установки Python локально)

```bash
# Запустите через Docker
docker-compose up -d

# Запустите агента в контейнере
docker-compose exec app python main.py
```

## Проверка установки

После установки зависимостей проверьте:

```bash
python healthcheck.py
```

Должны увидеть:
```
✓ Python version compatible
✓ Redis connection OK
✓ PostgreSQL connection OK
```

## Если всё равно не работает

Запустите демо-режим БЕЗ внешних зависимостей:

```bash
python demo.py
```

Демо симулирует работу агента в текстовом режиме без подключения к API.

---

**Рекомендация:** Для production используйте Python 3.11 или 3.12.
